# 改进执行进度

更新：2026-09-07。按确认的优先级执行；此文件仅记录项目实现与验收，不登记对话或团队知识库。

| 顺序 | 优先级 | 工作 | 状态 |
| --- | --- | --- | --- |
| 1 | P0 | 画像分母、队伍归属、缺失值可信度 | 实现已验收；新发现历史 4 图刀局污染，数据重建成为下一项 P0 |
| 2 | P0 | 离线 CI 与环境复现 | 已通过本地全新环境与 GitHub Actions Linux 验证（96 项测试及前端构建） |
| 3 | P0 | 公平消融 | 160 条冻结排序已有 AI 事后评分；历史数据修正后需重新冻结标签和比较 |
| 4 | P0 | 新比赛独立评测 | 新增 2 场 5 图首次 2/5 通过；修复后 111 回合全通过；检索盲测待完成 |
| 5 | P1 | 选手对比与样本差异 | 已实现并本地验收：跨队选择、样本组成、覆盖率及对手筛选修复 |
| 6 | P1 | 个体行为与结果关联 | 已实现并本地验收：六类行为分组、回合胜负与引用 |
| 7 | P1 | 有证据支持的画像总结 | 已实现并本地验收：结构化结论、分组引用和共用简报 |
| 8 | P1 | 人工盲评与模型增益验证 | 6 图 AI 事实审核及口径修复完成；独立人工质量评分仍未执行 |
| 9 | P1 | 预算与故障边界 | 共享本地账本与故障停止已验收；已有用户提供的余额快照，无实时接口 |
| 10 | P1 | 求职及申请展示材料 | 延后：先完成下述正式 benchmark 阶段 |
| 11 | P2 | 性能与模块优化 | 待实施 |
| 12 | P2 | 趋势与风格分析 | 待实施 |

## 当前执行顺序调整（2026-09-07）

正式 benchmark 优先于展示材料：补齐第 3、4、8 项的研究验证，再组织求职与申请叙事。先用既有开发样本校准标注规范和基线，再冻结新的整场测试比赛、问题、指标与运行预算。旧 held-out 和已看过的新比赛仍作回归，不重新标为未见样本。

见 [正式 benchmark 方案草案](BENCHMARK_PLAN.md)。样本规模是规划目标，尚未下载、冻结、标注或跑分；不得将方案描述为已完成实验。账户余额快照只保留本地，不提升现有调用上限。

2026-09-07 后续调整：按用户要求改为 Codex 辅助审核与 2 场新比赛试点。已有 16 题源码事实核对和 160 条冻结排序的事后评分，标签为 AI 身份；新系列在下载前冻结选择。144 项测试通过，详情见 [AI 审核记录](AI_BENCHMARK_REVIEW.md)。8–12 场正式研究规模仍未执行。

## 第一项验收

- 从本地 Demo 冻结结束时刻读取名单，补齐 49 张地图、1,023 个有效回合；全部回合有完整 10 人名单。迁移前自动 SQLite 备份，保留原有事件与边。
- 56 名选手合计 10,230 人次回合；名单与事件归属不一致为 0，画像列表与上下文分母不一致为 0。
- 按每场/每回合识别队伍，处理替补、跨队样本及无事件参赛回合。旧数据缺少名单时明确标注估算。
- 无样本和零分母的比例返回 null，页面显示“—”；无样本检索不生成选手简报。展示比赛 ID，日期尚未收录，不推断日期。
- 浏览器验证正常画像、样本来源展开与无样本筛选。名单快照不能证明回合中途换人的全部情况。

见 [画像数据契约](PLAYER_PROFILE_DATA_CONTRACT.md)、[名单覆盖](../datasets/evaluation/player_roster_coverage_v1.json)、[分母审计](../datasets/evaluation/player_scope_audit_v1.json)。

## 第二项验收

- 全新 Python 虚拟环境受约束安装成功，pip check 无冲突，87 项离线测试通过（1 条上游弃用警告）。
- 测试禁用配置密钥、自动入库与 Python socket 联网；Webhook 手动探测脚本不参与自动收集。
- 独立前端目录 npm ci 与生产构建成功；原有前端锁文件保留。
- 补充 workflow 权限后，正式工作流 `.github/workflows/ci.yml` 已启用；首次远端 Linux 验收 96 项测试通过，依赖检查与前端构建通过。[运行记录](https://github.com/Zzz0zzZ0/CS2-coach-agent/actions/runs/34031162026)。

见 [复现说明](OFFLINE_VALIDATION.md)。

## 本阶段回归快照

| 数据集 | Vector 查询通过 | Graph 查询通过 | Hybrid 查询通过 |
| --- | ---: | ---: | ---: |
| 开发集 | 50/50 | 50/50 | 50/50 |
| 既有 held-out 查询集 | 27/30 | 30/30 | 30/30 |

两次运行的结构化契约均为 50/50。Graph/Hybrid 检查点分别为 152/152 与 99/99；Vector held-out 为 96/99。该 held-out 已用于修复，且共享历史语料，本次是工程回归，不是新比赛盲测或公平研究消融。远程 LLM 调用关闭。

报告：[开发集](../datasets/evaluation/cs2_coach_player_p0_report.json)、[既有 held-out](../datasets/evaluation/cs2_coach_player_p0_holdout_report.json)。旧基线和失败记录保留。

## 第三、四项本阶段验收

- 新增同语料 BM25 / dense / RRF 实验和单列 SQL / 图路径参考，16 查询 × 5 方法 × 2 种实体条件 = 160 条结果。
- 标签未审核时默认拒绝正式评测；显式试跑不输出 Recall/nDCG 等相关性评分。实际模型上限 128 token，实验改为完整分块，并固定制品哈希。
- 下载前冻结独立比赛 2396950；两图 44 回合完整名单，解析比分与公开比分一致，复盘到 Verifier 全部通过。历史图谱未改变，模型调用为 0。
- 当前全套离线测试 96 项通过。见 [同语料实验](FAIR_RETRIEVAL_RUN.md) 与 [独立比赛首轮验收](UNSEEN_MATCH_PILOT.md)。
- 第 3 项仍缺独立相关性审核；第 4 项仍需更多整场样本及新语料检索盲测。上述试跑与单场验收不能代替研究结论。

## 第五项验收

- 跨队比较展示双方样本规模、K/D 原始计数和首杀机会数；完整参赛回合用于共同覆盖，不使用截断事件引用。
- 区分共同参赛与共同地图 / 阵营 / 对手条件，显示组成分布、覆盖比例及样本差异。没有做重加权或显著性推断。
- 修复自然语言对比丢弃对手筛选，以及筛选变化后保留旧对比的问题。未知明确对手不放宽检索。
- 全套 104 项离线测试、前端构建通过；56 名选手分母与组成审计通过；20 条既有选手查询通过。浏览器核验跨队选择、明细展开、无样本提示。
- 原始图谱不变，旧评测制品保留；此前未见比赛选择记录冻结了旧实现哈希，不为适配新代码改写历史冻结清单。

见 [数据口径与验收](PLAYER_PROFILE_DATA_CONTRACT.md)。第六项在下文记录；第 3、4 项的研究评测缺口继续保留。

## 第六项验收

- 首杀、首死、补枪、道具、致盲、下包与回合结果关联；按参赛回合去重，提供观测 / 未观测两组计数和胜率。
- 未知结果不进入胜率分母，无样本返回 null；按历史队伍判断胜负，引用按胜 / 负 / 未知分别保留，支持跳转时间线。
- 109 项离线测试、前端构建通过；56 名选手的六类行为原始记录审计通过，20 条既有查询通过，历史图谱未修改。
- 当前是未匹配样本的描述性关联，不输出因果、显著性或能力排名；第七项在下文记录。

见 [行为与结果口径及验收](PLAYER_BEHAVIOR_OUTCOMES.md)。

## 第七项验收

- 画像页与自然语言查询共用确定性简报，保留基础表现，并为行为结论记录原始数值、分母、范围与分组引用。
- 所问行为无记录时不回退到其他主题，移除主观可信度档位和未经证据支持的站位 / 协同诊断。
- 119 项离线测试、前端构建、26 条查询验收通过；浏览器核验自动总结、零行为样本与来源下钻。
- 这仍是工程证据一致性验收。下一项是人工盲评与模型增益验证；独立人员评分不可由系统自身冒充完成。

见 [总结契约与验收](PLAYER_GROUNDED_SUMMARY.md)。

## 第八项工程验收与待办

- 调用前在 `75c68e2` 冻结 6 图 / 5 场比赛的输入、代码哈希与协议；使用双方相同的指标与报告模板，仅比较训练优先级选择。
- 固定 `qwen3.8-flash` 完成 6 次调用，提供商报告本轮 4,939 token，调用延迟中位数约 1.43 秒；未重试、未降级。用量仅代表本轮，不代表账户剩余额度。
- 125 项离线测试与远端 CI 通过；匿名页面排版及引用目标检查通过。生成本地评审包和两份空白评分表，方法映射独立存放。
- 真实人工评分仍为 0 份完整记录，质量指标为 null。不同优先级选择不代表模型更好；这些开发样本不能支撑新比赛泛化结论。
- 待至少两位独立评审完成评分后汇总描述性结果。该外部依赖不阻止第九项预算与故障边界的工程工作。

见 [盲评协议](COACH_BLIND_EVALUATION.md)、[模型运行汇总](../datasets/evaluation/coach_blind_v1_run_report.json)、[待评分状态](../datasets/evaluation/coach_blind_v1_pending_review.json)。

## 第九项验收

- 生产 `get_llm()` 接入持久 SQLite 账本，同步 / 异步和工具调用共用原子预留；默认从启用时累计 30,000 token / 100 次尝试，更换密钥及重启不清零。
- 超时、取消、提供商拒绝、用量缺失与未结算请求停止后续调用；实际用量超过估算照实记录并停止，账本不可用时不发网络请求。保留规则分析。
- 页面展示本地预算、暂停原因、已报告和未结算用量；Coach 单次用量独立标注，未知显示“—”。
- 140 项离线测试与前端构建通过，含三进程争抢、崩溃后 pending 保留及完整分析链降级；浏览器验证状态刷新。此次真实模型调用为 0，历史图谱不变。
- 本地账本不代表账户免费余额，也不覆盖其他项目或机器；不自动对账解锁。原服务未重启，新保护需服务加载更新后生效。

见 [预算与故障边界](MODEL_BUDGET_BOUNDARIES.md)。后续先补正式 benchmark，再制作求职与申请展示材料；独立人工评分仍待补。

## 恢复点

恢复分支 `codex/recovery-20260906` 已推送到 origin，保留完整提交历史。`606446d` 保存此前项目实现与实验 WIP；`d8f92b5` 保存可推送的 CI 示例配置。后续每个验收阶段继续提交并 push。运行时数据、原始 Demo 与模型缓存保留本地，项目产物和校验和进入 Git。

### AI 审核后的 Coach 事实修复

148 项测试通过；49 图 / 1,023 回合与 6 个 Coach 样本原有指标无变化。新增完整阵容支持的攻守胜率分母、保留零胜局与未知结果，并把同队闪光表述修正为含自己的己方受闪。未新增模型调用，人工质量评分仍为空。见 [审核与复现](COACH_FACT_AUDIT.md)。

### 新比赛发现的解析边界问题

两场新系列首次出现 3 张地图检查失败，原报告保留。修复赛前刀局窗口和 NaVi 公共名称映射后，5 图 / 111 回合全通过，150 项测试通过，新增模型调用 0。历史库仍冻结原状；49 个 Demo 边界审核发现 4 图各混入一个刀局，下一项优先重建历史数据及派生索引，再更新 benchmark。详见 [失败与修复记录](NEW_MATCH_VALIDATION_V3.md)。
